name: Build Flutter Debug APK

# Memicu workflow setiap kali ada 'push' ke branch 'main'
on:
  push:
    branches:
      - main

jobs:
  build-apk:
    # Menggunakan runner Ubuntu versi terbaru
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout kode dari repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Set up Java (diperlukan oleh Gradle)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 3. Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable' # Mengganti 'flutter-version' dengan 'channel'
          cache: true 

      # 4. Create dummy .env file
      - name: Create dummy .env file
        run: echo "API_KEY=mock_key" > .env

      # 5. Install dependensi
      - name: Install Flutter dependencies
        run: flutter pub get

      # 6. Build APK Debug
      - name: Build Debug APK
        run: flutter build apk --debug

      # 7. Upload APK sebagai artifak
      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk

